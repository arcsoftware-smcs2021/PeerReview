extends layout

block content
    .reviews
        each review in reviews
            .assignment
                a(href=review.downloadLink) Download
            p Notes
            input(type="text", class="message")
            input(type="submit", class="submitReview", value="Submit")
