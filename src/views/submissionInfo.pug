extends layout

block content
    p= submissionCount + " submissions"
    input(id="review-input")
    h1 Reviews
    a(class = 'button' , href='/lti/select/' + assignment.course_id + "/" + assignment.id) Confirm
